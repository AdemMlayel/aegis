services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    command: python -m uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
    env_file:
      - .env.example
    environment:
      AEGISQA_ENV: compose
      AEGISQA_DATABASE_URL: postgresql://aegisqa:aegisqa@postgres:5432/aegisqa
      AEGISQA_EXECUTION_WORKER_BACKEND: celery
      AEGISQA_CELERY_BROKER_URL: redis://redis:6379/0
      AEGISQA_CELERY_RESULT_BACKEND: redis://redis:6379/1
      AEGISQA_CELERY_FALLBACK_TO_LOCAL: "true"
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      - aegis-generated:/app/generated
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/health/ready"]
      interval: 10s
      timeout: 3s
      retries: 10
      start_period: 10s
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy

  worker:
    build:
      context: .
      dockerfile: Dockerfile
    command: celery -A backend.workers.celery_app.celery_app worker --loglevel=info
    env_file:
      - .env.example
    environment:
      AEGISQA_ENV: compose
      AEGISQA_DATABASE_URL: postgresql://aegisqa:aegisqa@postgres:5432/aegisqa
      AEGISQA_EXECUTION_WORKER_BACKEND: celery
      AEGISQA_CELERY_BROKER_URL: redis://redis:6379/0
      AEGISQA_CELERY_RESULT_BACKEND: redis://redis:6379/1
    volumes:
      - .:/app
      - aegis-generated:/app/generated
    depends_on:
      redis:
        condition: service_healthy
      postgres:
        condition: service_healthy

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    environment:
      VITE_API_PROXY_TARGET: http://api:8000
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      api:
        condition: service_healthy

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: aegisqa
      POSTGRES_PASSWORD: aegisqa
      POSTGRES_DB: aegisqa
    ports:
      - "5432:5432"
    volumes:
      - aegis-postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U aegisqa -d aegisqa"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  aegis-generated:
  aegis-postgres:
